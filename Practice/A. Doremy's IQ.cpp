#include<bits/stdc++.h>
using namespace std;
bool brr[100005];
int main()
{
    int t;
    cin >> t;
    while (t--) 
    {
        int n, q;
        cin >> n >> q;
        int arr[n+5];
        for (int i = 0; i < n; i++)
        {
            cin >> arr[i];
        }

        
    }
    
}